---
- name: Test the sample filter plugin
  ansible.builtin.set_fact:
    msg: "{{ 'ansible-creator' | idocollection.main.sample_filter }}"

- name: Assert that the filter worked
  ansible.builtin.assert:
    that:
      - msg == 'Hello, ansible-creator'
